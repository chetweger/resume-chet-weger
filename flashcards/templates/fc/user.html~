{% extends "base.html" %}
{% block content %}

<div class = "header">
<h3>Review or create flashcard sets.</h3>
<a align="left" href="/user_preferences/">User Preferences</a>
<form align="right" method="post" action="/log_out/">{% csrf_token %}
        <input type ="submit" value ="Log Out">
</form>
</div>

<div class = "content">
    <h3>Sets Library:</h3>
    <ol id="notes">
        {% for object in set_list %}
        <li>
            <a href="{{ object.get_absolute_url }}">
            {{ object.setName }}
            </a>
            <form method="post" action="/user/set/{{object.setID}}/delete_set/">{% csrf_token %}
            <input type="submit" value="Delete">
            </form>
        </li>
        {% endfor %}
    </ol>

    <form method="post" action="/create_set/">{% csrf_token %}
    <label for="setName">Set Name</label>
    <input type="text" name="setName" id="setName">
    <input type="submit" value="Create New Set">
    </form>
</div>

{% endblock %}
